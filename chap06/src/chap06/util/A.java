package chap06.util;

// 클래스의 접근 제한

public class A {

  // 1) public
  // 전부 공개
  // 2)default
  // 같은 패키지에 소속된 클래스에서만 사용 가능
  // 3) protected
  // 같은 패키지 또는 자식 클래스에서 사용 가능
  // 4) private
  // 같은 클래스에서만 사용 가능





}
